version: '3.8'

services:
  # Go API Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: go-catetduit-app
    restart: unless-stopped
    ports:
      - "8082:8082"
    env_file:
      - .env

networks:
  default:
    driver: host

